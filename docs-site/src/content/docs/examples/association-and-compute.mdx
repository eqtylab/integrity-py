---
title: "Association and Compute Statements"
description: "This example creates assets, records a computation from an input dataset to an output dataset, and adds one association statement for each association type."
---

import CodeFence from '@eqtylab/docs/components/CodeFence.astro';
import associationAndCompute from '../../../../../examples/association-and-compute.py?raw';

This example creates assets, records a computation from an input dataset to an output dataset, and adds one association statement for each association type: `CERTIFIES`, `INCLUDES`, and `IS_INSTANCE_OF`. Each association includes CID, UUID, and DID predicates. It then exports the context as a manifest.

Source: `examples/association-and-compute.py`

<CodeFence code={associationAndCompute} lang="python" />
