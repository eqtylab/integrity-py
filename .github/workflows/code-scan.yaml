name: Code Scanning

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, ready_for_review, reopened, synchronize]
  schedule:
    - cron: '0 6 * * 1'  # Weekly Monday 6am UTC
  workflow_dispatch:

jobs:
  # PR scans: Fast, lightweight, no attestation
  security-scan-pr:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest

    permissions:
      contents: read

    concurrency:
      group: ${{ github.workflow }}-pr-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout Repo
        uses: eqtylab-actions/checkout@v4

      - name: Code & Security Scan → View results in Summary tab
        uses: eqtylab/vbuild-scan-action@master
        with:
          sarif: false
          attest: false
          install-trivy: true
          upload-artifact: false
          cargo-vendor: true
          veracode: true
          veracode-api-id: ${{ secrets.VERACODE_API_ID }}
          veracode-api-key: ${{ secrets.VERACODE_API_KEY }}
          rl-scanner: true
          rl-encoded-license: ${{ secrets.RLSECURE_ENCODED_LICENSE }}
          rl-site-key: ${{ secrets.RLSECURE_SITE_KEY }}

  # Main branch scans: Full attestation in TEE
  security-scan-main:
    # if: github.ref == 'refs/heads/main'  # Uncomment after testing
    runs-on: vbuild

    permissions:
      contents: read
      id-token: write  # Required for vbuild Sigstore signing

    steps:
      - name: Checkout Repo
        uses: eqtylab-actions/checkout@v4

      - name: Install Rust toolchain
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | \
            sh -s -- -y --default-toolchain stable --profile minimal --no-modify-path
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Code & Security Scan → View results in Summary tab
        uses: eqtylab/vbuild-scan-action@master
        with:
          sarif: true
          attest: true
          install-trivy: true
          signer: vcomp-notary
          artifact-name: scan-provenance
          results-artifact-name: scan-results
          license-scanning: true
          cargo-vendor: true
          veracode: true
          veracode-api-id: ${{ secrets.VERACODE_API_ID }}
          veracode-api-key: ${{ secrets.VERACODE_API_KEY }}
          rl-scanner: true
          rl-encoded-license: ${{ secrets.RLSECURE_ENCODED_LICENSE }}
          rl-site-key: ${{ secrets.RLSECURE_SITE_KEY }}
